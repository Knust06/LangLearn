include: AGENTS.md

# LangLearn

Language learning app — lessons (mdsvex), coding exercises (JSON), progress charts, side-by-side comparison.

Stack, key files, architecture, constraints: `knowledge/wiki/index.md`

## Start
```bash
npm install && npm run dev  # http://localhost:5173
npm run db:seed             # first run only (start.bat does this)
```
