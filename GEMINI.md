include: CLAUDE.md

LangLearn: SvelteKit 2 language learning app with SQLite vocab/exercise storage, mdsvex markdown lessons, and Chart.js progress charts. Active project — check src/routes/ before modifying any route. Server-only DB logic lives in src/routes-backend/; never import it from client components.
